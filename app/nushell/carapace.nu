# --- Carapace - autocompletion
# --- win: scoop install carapace-bin
# $env.CARAPACE_BRIDGES = "zsh,fish,bash,inshellisense" # Optional: bridge other shells
# In config.nu
#mkdir ($nu.default-config-dir | path dirname | path join "completions")
#carapace _carapace nushell | save --force ($nu.default-config-dir | path dirname | path join "completions/carapace.nu")
